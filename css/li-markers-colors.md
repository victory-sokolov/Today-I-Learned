# Li markers colors and size


The CSS ::marker pseudo-element can help us to change the color and size of a list bullet or number.

```css
.list li::marker {
  color: #fcf2db;
  font-size: 1.2em;
}
```
